import { AppRegistry } from 'react-native';
import App from './App';

console.disableYellowBox = true; // Disable React Native yellow box warnings

AppRegistry.registerComponent('myphone', () => App);
